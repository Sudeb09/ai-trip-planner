// import axios from "axios"


// const Base_URL = `https://api.gomap.pro/v1/photo?query=${placeName}&key=${API_KEY}`;

// const config = {
//   headers: {
//     "Content-Type": "application/json",
//     "X-Goog-Api-Key": import.meta.env.VITE_GOMAPS_API_KEY,
//     "X-Goog-FieldMask": "places.photos,places.displayName,places.id", // Fix: Use a comma-separated string
//   },
// };

// export const GetPlaceDetails = (data) => axios.post(Base_URL, data, config);